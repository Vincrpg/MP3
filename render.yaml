services:
  - type: web
    name: soundrip
    runtime: python
    buildCommand: |
      pip install -r requirements.txt
      apt-get install -y ffmpeg || true
    startCommand: python app.py
    envVars:
      - key: PORT
        value: 5000
    plan: free
